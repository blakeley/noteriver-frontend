{{#if model.scores}}
  <ul class='scores'>
    {{#each model.scores as |score|}}
      {{li-score score=score}}
    {{/each}}
  </ul>
{{else}}
  <p class="message">
    {{model.username}} doesn't have any scores yet
  </p>
{{/if}}
